# HY-LITE 单词网站 · 使用说明（老板版）

## 一、日常要用的三个地址

| 用途 | 地址 |
|---|---|
| 学生背单词 | **https://hylite.com.cn/vocabulary/** |
| 学生数据后台（看注册/进度） | **https://hylite.com.cn/admin/** |
| 代码仓库（备份，一般不用管） | https://github.com/Kimjin07/hylite |

- 管理后台密钥在本文件夹的 `ADMIN_KEY.txt` 里，**别外传、别提交到网上**
- 学生教程：打开网站 → 我的 → 云同步注册（填真实姓名）→ 截图保存同步码；换设备输同步码即恢复进度

## 二、文件夹结构

```
HY/
├── 使用说明.md            ← 本文件
├── ADMIN_KEY.txt          ← 管理后台密钥（保密！）
├── 必会3000-百词斩版.html   ← 单机版（双击可离线用，功能同网站）
├── 词表源文件/             ← 所有词表的原始 txt/docx
├── vocabulary/            ← 网站源码
│   ├── src/               ← 改功能改这里
│   ├── build.ps1          ← 单文件构建
│   ├── build-split.js     ← 线上版构建
│   └── tools/             ← 词表转换器 + 数据体检工具
├── functions/             ← 云同步后端（Cloudflare）
├── deploy/                ← 线上部署目录（构建产物，别手改）
├── tests/                 ← 自动化测试（107项）
├── 归档(可忽略)/           ← 旧文件和一次性脚本，不用管
└── schema.sql 等           ← 数据库结构和配置
```

## 三、常见操作（找 Claude 说一声就行，也可以自己跑）

**加一本新词书** —— 把词表 txt 发给 Claude，格式要求：
```
Unit 1            ← 单元标题单独一行（不含中文；或"Day 1"、"1. 场景名"）

word /wɜːd/ 中文释义 n.     ← 一行一词：单词 /音标/ 释义 词性
```

**改词库内容后发布上线：**
```powershell
node vocabulary/build-split.js
npx wrangler pages deploy .\deploy --project-name hylite --branch main --commit-dirty=true
```

**学生说进度丢了：** 管理后台 → 点该学生 → 下载备份（还有"上一版"按钮）→ 把 json 发给学生在"我的→导入进度"恢复；学生本机"我的→本机备份恢复"里通常也有

**改同步/登录相关代码后必须跑测试：**
```powershell
npx wrangler pages dev --port 8788    # 先起本地服务（另开窗口）
node tests/test-api.js                # 75 项后端测试
node tests/test-sync-client.js        # 32 项同步仿真
```

## 四、账号一览

| 服务 | 账号 | 用途 |
|---|---|---|
| Cloudflare | kimjin070320@gmail.com | 网站托管+域名解析+云同步数据库（全免费） |
| GitHub | Kimjin07 | 代码备份 |
| 腾讯云 | （你的微信登录） | 域名注册续费；COS 存储桶留作备份可删 |

## 五、待办池（需要时找 Claude）

- 公司主页（做好后放 hylite.com.cn 根路径）
- ICP 备案（买台¥112轻量服务器启动 → 解锁微信登录+微信小程序）
- 短信验证码登录（阿里云通道 1-2 天可上线）
- 免费 SSL 证书 ZErTXJVM 是早期腾讯云方案的遗留，现在用不上，可无视
